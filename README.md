# PPM colour generator
Simple program to generate colours in PPM format.

# How to build for Linux?
## Compile & Run locally:
1. Compile:
    - `./clean.sh`
    - `./build.sh`
2. Run locally(e.g, Ubuntu):
    - `./run.sh`
